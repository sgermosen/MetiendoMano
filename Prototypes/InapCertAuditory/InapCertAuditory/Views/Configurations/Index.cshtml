@model IEnumerable<InapCertAuditory.Models.Configuration>

@{
    ViewData["Title"] = "Index";
}

<h1> Listado </h1>

<p>
    <a asp-action="Create" class="btn btn-primary"> Adicionar Nuevo </a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
               Rnc
            </th>
            <th>
               Nombre
            </th>
           
            <th>
              Directora
            </th>
             
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Rnc)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            
            <td>
                @Html.DisplayFor(modelItem => item.Director)
            </td>
            
            <td>
                <a asp-action="Edit" asp-route-id="@item.ConfigurationId" class="btn btn-warning">Editar</a> |
                <a asp-action="Details" asp-route-id="@item.ConfigurationId" class="btn btn-info">Detalles</a> |
                <a asp-action="Delete" asp-route-id="@item.ConfigurationId" class="btn btn-danger">Eliminar</a>
            </td>
        </tr>
}
    </tbody>
</table>
