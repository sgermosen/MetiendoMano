@model IEnumerable<InapCertAuditory.Models.CourseSectionMember>

@{
    ViewData["Title"] = "Index";
}

<h1> Listado </h1>

<p>
    <a asp-action="Create" class="btn btn-primary"> Adicionar Nuevo </a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                Nombre
            </th>
            <th>
               Observaciones
            </th>
            <th>
            Puntuacion
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Participant.FullName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Observations)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Punctuation)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.CourseSectionMemberId" class="btn btn-warning">Editar</a> |
                <a asp-action="Details" asp-route-id="@item.CourseSectionMemberId" class="btn btn-info">Detalles</a> |
                <a asp-action="Delete" asp-route-id="@item.CourseSectionMemberId" class="btn btn-danger">Eliminar</a>
            </td>
        </tr>
}
    </tbody>
</table>
